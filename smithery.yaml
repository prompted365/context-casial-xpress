name: "meta-orchestration-protocol"
title: "Meta-Orchestration Protocol (MOP)"
description: "Consciousness-aware MCP orchestration framework that acts as a proxy layer to augment any MCP server's tools with context injection, swarm instructions, and paradox handling."
vendor: "Prompted LLC"
homepage: "https://github.com/prompted365/context-casial-xpress"
runtime: "container"
startCommand:
  type: "http"
  configSchema:
    type: "object"
    required: ["apiKey"]
    properties:
      apiKey:
        type: "string"
        title: "API Key"
        description: "Your API key for authentication (DEMO KEY – public). Override via the MOP_API_KEY environment variable."
        default: "DEMO_KEY_PUBLIC"
      agent_role:
        type: "string"
        title: "Agent Role"
        description: "Role of the calling agent for context-aware responses"
        enum: ["researcher", "analyst", "monitor", "watcher", "orchestrator"]
        default: "orchestrator"
      consciousness_mode:
        type: "string"
        title: "Consciousness Mode"
        description: "Level of consciousness integration"
        enum: ["full", "partial", "disabled"]
        default: "full"
      max_context_size:
        type: "integer"
        title: "Max Context Size"
        description: "Maximum context size in characters"
        minimum: 1000
        maximum: 1000000
        default: 100000
      mission:
        type: "string"
        title: "Mission Profile"
        description: "Pre-configured mission to load"
        enum: ["exa-orchestration", "general", "research", "monitoring"]
        default: "exa-orchestration"
      shim_enabled:
        type: "boolean"
        title: "Enable Pitfall Avoidance Shim"
        description: "Enable automatic context injection and pitfall warnings"
        default: true
      debug:
        type: "boolean"
        title: "Debug Mode"
        description: "Enable debug logging"
        default: false
  exampleConfig:
    apiKey: "DEMO_KEY_PUBLIC"
    agent_role: "researcher"
    consciousness_mode: "full"
    max_context_size: 50000
    mission: "exa-orchestration"
    shim_enabled: true
    debug: false
  commandFunction: |
    module.exports = async function ({ env, config }) {
      const apiKey = env.MOP_API_KEY ?? config.apiKey ?? 'DEMO_KEY_PUBLIC';
      return {
        headers: {
          Authorization: 'Bearer ' + apiKey,
        },
      };
    };
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
env:
  RUST_LOG: "info"
  CONSCIOUSNESS_ENABLED: "true"
  SUBSTRATE_INTEGRATION: "true"
  PORT: "8081"
metadata:
  transport: "streamable-http"
  capabilities:
    tools: true
    prompts: true
    resources: true
    sampling: true
  documentation: "https://github.com/prompted365/context-casial-xpress/blob/main/ORCHESTRATED_SWARMS_WHITEPAPER.md"
  tags:
    - orchestration
    - swarm
    - mcp
    - proxy
    - context-aware
    - multi-agent
    - consciousness
    - paradox